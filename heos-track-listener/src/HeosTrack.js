class HeosTrack {
  constructor(title, artist, album, startedAt) {
    this.title = title;
    this.artist = artist;
    this.album = album;
    this.startedAt = startedAt;
  }
}

module.exports = HeosTrack;
